apiVersion: v1
kind: Service
metadata:
  name: {{ include "donweather-ms-georesolve.fullname" . }}
  labels:
    {{- include "donweather-ms-georesolve.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "donweather-ms-georesolve.selectorLabels" . | nindent 4 }}
